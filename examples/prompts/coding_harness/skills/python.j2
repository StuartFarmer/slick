Python workflow:
Inspect the project's configuration and nearby tests. Preserve public signatures
and declared version support. Prefer small functions and standard-library behavior.
Reproduce a failure when practical, make a focused edit, run the relevant checks,
and distinguish environment failures from failing code. Do not silently remove
assertions or skip failing tests to make verification pass.
