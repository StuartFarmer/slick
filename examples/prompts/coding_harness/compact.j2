Summarize this coding conversation so work can continue without rereading it.
Keep the user's goal, important decisions, files changed, actual check failures,
and the next step. Never claim unfinished work is complete. Return only the
handoff note, without tools.

{% for item in history %}
{{ item.role }}: {{ item.text }}
{% if item.get("calls") %}Tool requests: {{ item.calls | tojson }}{% endif %}
{% endfor %}
